# EroDir
A fast web directory/file enumeration tool written in Rust

# [Install & Compile]
1. Install Rust - https://www.rust-lang.org/tools/install
2. cd EroDir
3. cargo build --release
4. target/release/erodir --help
